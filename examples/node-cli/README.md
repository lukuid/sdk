# Node CLI Example

Simple CLI placeholder that shells into `@lukuid/node` and prints TODO steps.

## Run

```bash
pnpm install --filter @lukuid/node --filter @lukuid/examples-node-cli
pnpm --filter @lukuid/examples-node-cli start
```
