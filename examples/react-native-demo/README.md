# React Native Demo

Placeholder directory for future React Native packaging work.

## Run

There is no published `@lukuid/react-native` package at the moment.
Use the native iOS and Android SDKs directly until a dedicated React Native wrapper exists.
