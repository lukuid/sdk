# Web Demo

Minimal Vite skeleton that imports `@lukuid/web` and renders a stub UI.

## Run

```bash
pnpm install --filter @lukuid/web --filter @lukuid/examples-web-demo
pnpm --filter @lukuid/examples-web-demo dev
```
