# Exporting

When used with valid LukuID devices, the SDK may export `.luku` files.

Export behavior should preserve:

- record integrity
- native continuity within blocks
- honest handling of continuity gaps
- deterministic block hashing
- correct manifest sealing
