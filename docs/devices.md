# Devices

The SDK can communicate with valid LukuID devices depending on platform and transport support.

Typical responsibilities include:

- forwarding commands
- receiving signed outputs
- exporting device-originated data
- verifying returned data offline

The SDK does not replace the device trust model.
